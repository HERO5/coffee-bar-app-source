<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android">
    <group android:checkableBehavior="single">
        <item
            android:id="@+id/left_menu_main"
            android:checked="false"
            android:icon="@mipmap/ic_home_black_24dp"
            android:title="@string/left_menu_main"/>

        <item
            android:id="@+id/left_menu_bake_studio"
            android:checked="false"
            android:icon="@mipmap/ic_school_black_24dp"
            android:title="@string/left_menu_bake_studio"/>
        <!--
        <item
            android:id="@+id/left_menu_categories"
            android:checked="false"
            android:icon="@mipmap/ic_dashboard_black_24dp"
            android:title="@string/left_menu_categories"/>

        <item
            android:id="@+id/left_menu_bake_shop"
            android:checked="false"
            android:icon="@mipmap/ic_cart_black_24dp"
            android:title="@string/left_menu_bake_shop"/>
        -->
        <item
            android:id="@+id/left_menu_personal"
            android:checked="false"
            android:icon="@mipmap/ic_account_black_24dp"
            android:title="@string/left_menu_personal"/>
    </group>
</menu>